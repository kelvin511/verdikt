## What does this change?

## Why?

## How was this tested?

- [ ] `npm run build` passes from the repo root
- [ ] Ran the affected command(s) locally (`verdikt-adr scan` / `verdikt-adr serve`) and checked the output
- [ ] Updated the README's CLI reference table, if a flag changed

## What does this change?

## Why?

## How was this tested?

- [ ] `npm run build` passes from the repo root
- [ ] Ran the affected command(s) locally (`verdikt scan` / `verdikt serve`) and checked the output
- [ ] Updated the README's CLI reference table, if a flag changed
